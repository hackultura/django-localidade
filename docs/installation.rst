============
Installation
============

At the command line::

    $ easy_install django-localidade

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-localidade
    $ pip install django-localidade
